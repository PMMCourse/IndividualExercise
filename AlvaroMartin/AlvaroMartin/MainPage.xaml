<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AlvaroMartin"
             x:Class="AlvaroMartin.MainPage">
    <StackLayout>
        <Label Text="Mis Posts" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />
        <SearchBar TextChanged="SearchBar_TextChanged"></SearchBar>
        <ListView x:Name="Post_List" ItemTapped="MainListView_ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding title}" Detail="{Binding body}"></TextCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>